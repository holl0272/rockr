=form_for @venue, :url => venues_path, :remote => true do |f|
  = f.label :name
  = f.text_field :name, :autofocus => true
  = f.submit "Save"
  = link_to_function('Cancel', 'cancel_form()', :class => 'cancel')
